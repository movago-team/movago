# Sandbox (experiments only)

Work in this folder is **not production**. Do not merge sandbox pages into marketing routes without refactoring into `components/`, `services/`, and `app/(marketing)/`.

Put personal experiments here only.
